#include <stdio.h>

int main(void) {
{
    long long m;
    int count = 0;

    printf("Enter an integer: ");
    scanf("%lld", &m);

    while(n != 0)
    {
      
        n /= 10;
        ++count;
    }

    printf("Number of digits %d", count);
}
	return 0;
}
